# Screenshots

Add portfolio screenshots here before publishing to GitHub:

1. **dashboard.png** — logged-in dashboard with stats
2. **matches.png** — similarity-ranked match results with % scores
3. **notifications.png** — real-time WebSocket toast notification

Capture at 1280×800 or similar. Reference them in the main README:

```markdown
![Dashboard](docs/screenshots/dashboard.png)
```
